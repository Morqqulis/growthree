import type { Metadata } from 'next'

const homeMetadata: Metadata = {
	title: 'GROW TREE',
	description: 'GROW TREE is a web application for planting trees with beautifull scroll animation.',
}

export { homeMetadata }
